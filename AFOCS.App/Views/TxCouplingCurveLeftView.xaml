<base:CouplingCurveViewBase x:Class="AFOCS.App.Views.TxCouplingCurveLeftView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:base="clr-namespace:AFOCS.App.Views"
             xmlns:sp="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="6,4">
            <TextBlock Text="通道:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ItemsControl ItemsSource="{Binding ChannelToggles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Display}"
                                  IsChecked="{Binding IsVisible}"
                                  Margin="0,0,10,0"
                                  VerticalAlignment="Center"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
        <sp:WpfPlot x:Name="PlotControl" />
    </DockPanel>
</base:CouplingCurveViewBase>
