<UserControl x:Class="AFOCS.App.Views.Settings.RotationCompensationSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://caliburnmicro.com"
             xmlns:vm="clr-namespace:AFOCS.App.ViewModels.Settings"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:RotationCompensationSettingsViewModel}">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button Content="保存" cal:Message.Attach="SaveAsync" Width="70" Height="26"/>
            <TextBlock Text="{Binding StatusMessage}" Margin="10,6,0,0" VerticalAlignment="Center" Foreground="Gray"/>
        </StackPanel>

        <TextBlock DockPanel.Dock="Top" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="Gray"
                   Text="夹爪旋转中心偏离自身中心，绕轴旋转后需补偿。绕X轴→影响YZ平面（用Y、Z配置），绕Y轴→影响XZ平面（用X、Z配置），绕Z轴→影响XY平面（用X、Y配置）。"/>

        <Grid Margin="0,12,0,0" HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="130"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="轴" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="初始角度(度)" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="0" Grid.Column="2" Text="旋转半径(um)" FontWeight="Bold" VerticalAlignment="Center"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="X 轴" VerticalAlignment="Center" Margin="0,6,0,0"/>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="4,6,4,0" Text="{Binding XInitialAngle, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Grid.Row="1" Grid.Column="2" Margin="4,6,4,0" Text="{Binding XRadius, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Y 轴" VerticalAlignment="Center" Margin="0,6,0,0"/>
            <TextBox Grid.Row="2" Grid.Column="1" Margin="4,6,4,0" Text="{Binding YInitialAngle, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Grid.Row="2" Grid.Column="2" Margin="4,6,4,0" Text="{Binding YRadius, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Z 轴" VerticalAlignment="Center" Margin="0,6,0,0"/>
            <TextBox Grid.Row="3" Grid.Column="1" Margin="4,6,4,0" Text="{Binding ZInitialAngle, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Grid.Row="3" Grid.Column="2" Margin="4,6,4,0" Text="{Binding ZRadius, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
    </DockPanel>
</UserControl>
