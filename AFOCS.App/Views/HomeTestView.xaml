<UserControl x:Class="AFOCS.App.Views.HomeTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://caliburnmicro.com"
             xmlns:vm="clr-namespace:AFOCS.App.ViewModels"
             xmlns:infrastructure="clr-namespace:AFOCS.Infrastructure;assembly=AFOCS.Infrastructure"
             xmlns:converter="clr-namespace:AFOCS.App.Converter"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:HomeTestViewModel}"
             Background="#F3F3F3">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converter:EnumToCollectionConverter x:Key="EnumToCollection"/>

        <SolidColorBrush x:Key="AccentBrush" Color="#0078D4"/>
        <SolidColorBrush x:Key="CardBg" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="CardBorder" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#888888"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
        <StackPanel>

            <!-- ===== 工位 + 回零按钮 ===== -->
            <Border Background="{StaticResource CardBg}"
                    BorderBrush="{StaticResource CardBorder}" BorderThickness="1"
                    CornerRadius="8" Padding="8,6" Margin="0,0,0,6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="总线轴工位:" FontSize="12"
                                   Foreground="{StaticResource TextSecondary}"
                                   VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <ListBox ItemsSource="{Binding Converter={StaticResource EnumToCollection},
                                      ConverterParameter={x:Type infrastructure:WorkPos}}"
                                 DisplayMemberPath="Description"
                                 SelectedValuePath="Value"
                                 SelectedValue="{Binding SelectedStation}"
                                 BorderThickness="0" Background="Transparent">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Width" Value="72"/>
                                    <Setter Property="Height" Value="28"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                    <Setter Property="Background" Value="{StaticResource CardBorder}"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Margin" Value="0,0,8,0"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="bd" Background="{TemplateBinding Background}"
                                                        CornerRadius="6">
                                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="bd" Property="Background" Value="#EBEBEB"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <ProgressBar Width="60" Height="4" IsIndeterminate="True"
                                     Margin="0,0,8,0" VerticalAlignment="Center"
                                     Visibility="{Binding IsHoming, Converter={StaticResource BoolToVis}}"/>
                        <Button cal:Message.Attach="HomeSelected" Content="回零选中轴"
                                Width="88" Height="28" FontSize="11" FontWeight="SemiBold"
                                Foreground="White" Background="#D35400" BorderThickness="0" Cursor="Hand"
                                IsEnabled="{Binding HasSelection}">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ===== 总线轴列表 ===== -->
            <Border Background="{StaticResource CardBg}"
                    BorderBrush="{StaticResource CardBorder}" BorderThickness="1"
                    CornerRadius="8" Padding="8,6" Margin="0,0,0,6">
                <StackPanel>
                    <TextBlock Text="总线轴" FontSize="12" FontWeight="SemiBold"
                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,4"/>
                    <ItemsControl ItemsSource="{Binding BusAxes}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8F8F8" CornerRadius="4" Padding="6,3" Margin="0,0,0,2">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}"
                                                  VerticalAlignment="Center" Margin="0,0,6,0"
                                                  cal:Message.Attach="[Event Checked] = [Action OnSelectionChanged()];
                                                                        [Event Unchecked] = [Action OnSelectionChanged()]"/>

                                        <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="11"
                                                   Foreground="{StaticResource TextPrimary}"
                                                   VerticalAlignment="Center"/>

                                        <ProgressBar Grid.Column="2" Width="40" Height="4"
                                                     IsIndeterminate="True"
                                                     Visibility="{Binding IsHoming, Converter={StaticResource BoolToVis}}"
                                                     VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- ===== 雅克贝斯轴列表 ===== -->
            <Border Background="{StaticResource CardBg}"
                    BorderBrush="{StaticResource CardBorder}" BorderThickness="1"
                    CornerRadius="8" Padding="8,6" Margin="0,0,0,6">
                <StackPanel>
                    <TextBlock Text="雅克贝斯" FontSize="12" FontWeight="SemiBold"
                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,4"/>
                    <ItemsControl ItemsSource="{Binding AkAxes}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8F8F8" CornerRadius="4" Padding="6,3" Margin="0,0,0,2">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}"
                                                  VerticalAlignment="Center" Margin="0,0,6,0"
                                                  cal:Message.Attach="[Event Checked] = [Action OnSelectionChanged()];
                                                                        [Event Unchecked] = [Action OnSelectionChanged()]"/>

                                        <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="11"
                                                   Foreground="{StaticResource TextPrimary}"
                                                   VerticalAlignment="Center"/>

                                        <ProgressBar Grid.Column="2" Width="40" Height="4"
                                                     IsIndeterminate="True"
                                                     Visibility="{Binding IsHoming, Converter={StaticResource BoolToVis}}"
                                                     VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- ===== 状态栏 ===== -->
            <Border Background="{StaticResource CardBg}"
                    BorderBrush="{StaticResource CardBorder}" BorderThickness="1"
                    CornerRadius="8" Padding="6,5">
                <TextBlock Text="{Binding StatusText}" FontSize="10"
                           Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"
                           TextWrapping="Wrap"/>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
